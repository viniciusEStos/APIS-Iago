class Imagem {
    constructor(id, titulo, descricao, data_criacao) {
        this.id = id;
        this.titulo = titulo;
        this.descricao = descricao;
        this.data_criacao = data_criacao;
    }
}

module.exports = Imagem;